services:
  - type: web
    name: idle-duelist-backend
    env: python
    buildCommand: pip install -r requirements_free.txt
    startCommand: python free_deployment.py
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8000
